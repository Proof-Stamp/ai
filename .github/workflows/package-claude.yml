name: package-claude-skill

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to attach the Claude skill package to
        required: true
  release:
    types: [published]
  pull_request:
    paths:
      - "proofstamp/**"
      - "scripts/build_claude_skill.py"
      - "tests/test_claude_package.py"
      - ".github/workflows/package-claude.yml"

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository with history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set package source
        id: package_source
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            echo "ref=${{ github.event.release.tag_name }}" >> "$GITHUB_OUTPUT"
            echo "label=${{ github.event.release.tag_name }}" >> "$GITHUB_OUTPUT"
            echo "attach_release=true" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "ref=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
            echo "label=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
            echo "attach_release=true" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${{ github.event.pull_request.head.sha }}" >> "$GITHUB_OUTPUT"
            echo "label=pr-${{ github.event.pull_request.number }}" >> "$GITHUB_OUTPUT"
            echo "attach_release=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Use canonical ProofStamp source from selected ref
        shell: bash
        run: |
          git rev-parse --verify "${{ steps.package_source.outputs.ref }}"
          git checkout "${{ steps.package_source.outputs.ref }}" -- proofstamp

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build Claude skill package
        run: python scripts/build_claude_skill.py

      - name: Validate complete ZIP layout
        run: |
          python - <<'PY'
          from zipfile import ZipFile

          path = 'dist/claude/proofstamp.zip'
          required = {
              'proofstamp/skill.md',
              'proofstamp/references/TRUST-MODEL.md',
              'proofstamp/references/FORMAT.md',
              'proofstamp/references/PRIVACY.md',
              'proofstamp/references/PLATFORM-CAPABILITIES.md',
              'proofstamp/references/platforms/claude.md',
              'proofstamp/schemas/proofstamp-session-v1.schema.json',
              'proofstamp/schemas/proofstamp-receipt-v1.schema.json',
              'proofstamp/scripts/finalize_proofstamp.py',
              'proofstamp/scripts/validate_proofstamp.py',
              'proofstamp/scripts/create_receipt.py',
              'proofstamp/scripts/verify_proofstamp.py',
              'proofstamp/scripts/create_mailto.py',
          }
          with ZipFile(path) as zf:
              names = set(zf.namelist())
              missing = sorted(required - names)
              assert not missing, f'missing required Claude skill files: {missing}'
              assert 'proofstamp/SKILL.md' not in names
              assert all(name.startswith('proofstamp/') for name in names)
          print(f'{path}: complete ({len(names)} files)')
          PY

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: proofstamp-claude-skill-${{ steps.package_source.outputs.label }}
          path: dist/claude/proofstamp.zip
          if-no-files-found: error

      - name: Attach package to release
        if: steps.package_source.outputs.attach_release == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.package_source.outputs.ref }}
        run: gh release upload "$TAG" dist/claude/proofstamp.zip --clobber
